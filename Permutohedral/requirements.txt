# Permutohedral site percolation — fully standalone (no ATEAMS needed).
# Tested with Python 3.9.6 on the exact versions below.
numpy==2.0.2
scipy==1.13.1
gudhi==3.11.0
matplotlib==3.9.4
seaborn==0.13.2
